# Secret — dados sensíveis (senha do banco e URI de conexão).
# Em produção NUNCA versione o valor real no Git — este é um valor de aula.
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
stringData:
  MONGO_PASSWORD: "senha123"
  MONGO_URI: "mongodb://admin:senha123@mongo:27017/livraria?authSource=admin"
